import { DropDown, Carousel, MobileMenu } from './dynamicComponents';
import './sass/main.scss';

const items = ['Item 1', 'Item 2', 'Item 3', 'Item 4', 'Item 5'];

const dropDown = new DropDown(items);
